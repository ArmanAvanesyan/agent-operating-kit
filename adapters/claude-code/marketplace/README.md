# Claude Code Marketplace Compatibility

This directory preserves a Claude Code marketplace entrypoint for content that
originated in `claude-code-toolbox`.

The marketplace file points at AOK pack assets under `packs/*/claude-code/`.
Those assets remain Claude Code-specific compatibility projections; the pack
manifest beside each pack is the AOK source index.
